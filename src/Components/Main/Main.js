import React, { useState } from 'react'

import { View, Text } from 'react-native'

export default function Main(){
    return(
        <View style={{flex: 1}}>
            <Text> Teste </Text>
        </View>
    )
}